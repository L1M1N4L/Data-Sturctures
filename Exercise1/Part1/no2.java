package Exercise1.Part1;

    public void no2(int[] n) {
    /**
     * Prints all elements of the input integer array.
     * Time complexity: O(n) (linear).
     */
        for (int i = 0; i < n.length; i++) { // O(n)
            System.out.print(n[i]); // O(1)
        }
    }

